# Random long secret used to sign the session JWT cookie. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# Port the web server listens on
PORT=3000

# Where downloaded files are written by default
DOWNLOAD_DIR=./downloads
